node_modules/
dist/
.env
.env.*
*.log
*.zip
deploy/
writer.py
*.zip

# build/deploy artifacts — one-time appliers, backups, root staging files
*.bak
apply_*.py
/Coach.js

# zip delivery scaffolding — never commit
patch.py
APPLY_*.md
zip*_*.patch
zip*.zip

# zip delivery scaffolding — never commit
patch.py
APPLY_*.md
zip*_*.patch
zip*.zip

# zip delivery artifacts (patcher inputs — never repo content)
*_edits.json
apply_*.py
APPLY_FIELD_FIXES.md
