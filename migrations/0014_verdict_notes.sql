-- 0014: the verdict performance card — the director's notes persist with the run
-- (feeds the growth ledger, #17; winning transcripts become keepsakes later)
alter table z.roleplay_runs add column if not exists notes text;
