diff --git a/src/wanderer.ts b/src/wanderer.ts
index 13bdfaf..3e4396e 100644
--- a/src/wanderer.ts
+++ b/src/wanderer.ts
@@ -113,8 +113,10 @@ Resolve relative dates ("next month", "a week in December") to real calendar dat
   // 'planned' — leave the trip 'dreaming' rather than write a hollow plan.
   const built = Array.isArray(itinerary) && itinerary.length > 0;
   if (!built) {
-    // an async build that produced nothing must not stay stuck on 'planning'
-    await supabase.from('trip_files').update({ status: 'dreaming' })
+    // an async build that produced nothing must not stay stuck on 'planning' — and
+    // must not downgrade a trip that already has a real plan from an earlier build.
+    const revert = Array.isArray(trip.itinerary) && trip.itinerary.length ? 'planned' : 'dreaming';
+    await supabase.from('trip_files').update({ status: revert })
       .eq('id', tripId).eq('user_id', userId).eq('status', 'planning');
     return { ...trip, built: false };
   }
@@ -153,11 +155,19 @@ export async function startTripBuild(userId: string, tripId: string): Promise<an
     .select('id, destination, dates, travelers, notes, status, start_date, end_date, itinerary, checklist, budget, shop_cards, updated_at')
     .eq('id', tripId).eq('user_id', userId).maybeSingle();
   if (!trip) return null;
-  await supabase.from('trip_files').update({ status: 'planning', updated_at: new Date().toISOString() })
-    .eq('id', tripId).eq('user_id', userId);
+  // [0055-fix2] ATOMIC CLAIM — only the caller that flips it OUT of 'planning' fires
+  // the build. A double-tap or concurrent call gets null here and just returns
+  // "already building" — no second build stomps a good result.
+  const { data: claimed } = await supabase.from('trip_files')
+    .update({ status: 'planning', updated_at: new Date().toISOString() })
+    .eq('id', tripId).eq('user_id', userId).neq('status', 'planning')
+    .select('id').maybeSingle();
+  if (!claimed) return { ...trip, status: 'planning', building: true };   // a build is already in flight
   buildTrip(userId, tripId).catch(async (e: any) => {
     console.error('[trip] async build failed:', e?.message || e);
-    await supabase.from('trip_files').update({ status: 'dreaming' })
+    // never downgrade a trip that already has a plan; only release a never-planned one
+    const revert = Array.isArray(trip.itinerary) && trip.itinerary.length ? 'planned' : 'dreaming';
+    await supabase.from('trip_files').update({ status: revert })
       .eq('id', tripId).eq('user_id', userId).eq('status', 'planning');
   });
   return { ...trip, status: 'planning', building: true };
